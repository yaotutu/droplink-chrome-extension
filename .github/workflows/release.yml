name: Release

# 触发条件：创建以 v 开头的 tag（如 v1.0.0）
on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 设置 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 3. 设置 pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      # 4. 获取 pnpm store 目录
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      # 5. 缓存依赖
      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      # 6. 安装依赖
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 7. 获取版本号（去掉 v 前缀）
      - name: Get version
        id: version
        run: |
          TAG_NAME="${{ github.ref_name }}"
          VERSION="${TAG_NAME#v}"  # 去掉 v 前缀: v1.0.0 -> 1.0.0
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "📦 Release version: $VERSION"

          # 验证版本号格式（必须是纯数字格式，如 1.0.0）
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ 错误: 版本号格式不正确"
            echo "   Tag: $TAG_NAME"
            echo "   版本号必须是纯数字格式，如: v1.0.0, v2.1.3"
            exit 1
          fi

          # 更新 package.json 的版本号
          npm version $VERSION --no-git-tag-version --allow-same-version

      # 8. 构建扩展
      - name: Build extension
        run: pnpm build

      # 9. 打包扩展
      - name: Package extension
        run: pnpm package

      # 10. 重命名 zip 文件
      - name: Rename package
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mv build/chrome-mv3-prod.zip build/droplink-v${VERSION}.zip

      # 11. 创建 Release
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: build/droplink-v${{ steps.version.outputs.version }}.zip
          body: |
            ## Droplink Chrome Extension ${{ steps.version.outputs.tag }}

            ### 📦 安装方法
            1. 下载 `droplink-v${{ steps.version.outputs.version }}.zip`
            2. 解压缩文件
            3. 打开 Chrome 浏览器，进入 `chrome://extensions/`
            4. 开启右上角的"开发者模式"
            5. 点击"加载已解压的扩展程序"
            6. 选择解压后的文件夹

            ### 📝 更新日志
            请查看提交记录。

            ### ℹ️ 版本信息
            - 版本号: ${{ steps.version.outputs.tag }}
            - 构建时间: ${{ github.event.head_commit.timestamp }}
            - Commit: ${{ github.sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
