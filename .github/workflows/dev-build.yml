name: Chrome Extension Dev Build

on:
  # 监听 dev 分支的推送
  push:
    branches:
      - dev

  # 允许手动触发
  workflow_dispatch:

# 授予 GitHub Actions 创建 Release 的权限
permissions:
  contents: write

jobs:
  build:
    name: 构建 Dev 版本
    uses: ./.github/workflows/build-shared.yml
    permissions:
      contents: write
    with:
      branch_name: dev
      release_tag: dev-latest
      release_title: "🚀 Dev 开发版 (最新)"
      use_commit_count: true
      file_prefix: "droplink-dev-"
      is_prerelease: true
