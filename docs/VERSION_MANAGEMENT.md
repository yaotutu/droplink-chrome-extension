# 版本号管理指南

## 📋 版本号策略

### 单一数据源原则
**`package.json` 是唯一的版本号来源**

所有版本号都从 `package.json` 的 `version` 字段读取，确保一致性。

---

## 🔄 版本号流程

### 1. 开发版 (dev-latest)
**触发**: 推送到 `dev` 分支

**版本号**: 基于 Git 提交总数（自动递增）
```
1.0.42-dev  (42 是提交总数)
```

**用途**:
- 日常开发测试
- 快速迭代
- 自动构建

---

### 2. 正式版 (main-latest) ⭐
**触发**: 推送到 `main` 分支

**版本号**: 从 `package.json` 读取
```json
{
  "version": "1.0.0"
}
```

**文件名**: `droplink-v1.0.0.zip`

**用途**:
- 稳定发布
- 用户下载
- 扩展显示的版本号

---

### 3. 特定版本 Release (v1.0.0)
**触发**: 推送 Git Tag `v*`

**版本号**: 从 Git Tag 读取
```bash
git tag v1.0.0
git push origin v1.0.0
```

**文件名**: `droplink-v1.0.0.zip`

**用途**:
- 里程碑版本
- 历史版本归档
- 不会覆盖 main-latest

---

## 📝 如何发布新版本

### 方法 1: 自动发布（推荐）

1. **修改版本号**
   ```bash
   # 编辑 package.json
   {
     "version": "1.1.0"  # 从 1.0.0 改为 1.1.0
   }
   ```

2. **提交并推送到 main**
   ```bash
   git add package.json
   git commit -m "chore: bump version to 1.1.0"
   git push origin main
   ```

3. **GitHub Actions 自动构建**
   - 读取 package.json 的版本号 `1.1.0`
   - 构建扩展
   - 发布到 `main-latest` Release
   - 文件名: `droplink-v1.1.0.zip`

4. **用户看到的版本号**
   - 扩展设置页面: `v1.1.0`
   - 下载文件名: `droplink-v1.1.0.zip`
   - **完全一致！** ✅

---

### 方法 2: 使用 npm version 命令

```bash
# 自动更新 package.json 并创建 git commit
npm version patch  # 1.0.0 -> 1.0.1
npm version minor  # 1.0.0 -> 1.1.0
npm version major  # 1.0.0 -> 2.0.0

# 推送到 main
git push origin main
```

---

### 方法 3: 创建特定版本 Tag

```bash
# 1. 确保 package.json 版本号正确
# 2. 创建并推送 tag
git tag v1.0.0
git push origin v1.0.0

# 这会触发 release.yml，创建独立的版本 Release
```

---

## 🎯 版本号规范

### 语义化版本 (Semantic Versioning)

格式: `MAJOR.MINOR.PATCH`

- **MAJOR**: 重大更新，不兼容的 API 变更 (1.0.0 → 2.0.0)
- **MINOR**: 新功能，向后兼容 (1.0.0 → 1.1.0)
- **PATCH**: Bug 修复，向后兼容 (1.0.0 → 1.0.1)

### 示例

| 变更类型 | 版本变化 | 说明 |
|---------|---------|------|
| 修复 Bug | 1.0.0 → 1.0.1 | PATCH |
| 新增功能 | 1.0.0 → 1.1.0 | MINOR |
| 重大重构 | 1.0.0 → 2.0.0 | MAJOR |

---

## 📊 版本号对比表

| 位置 | 版本号来源 | 示例 | 一致性 |
|------|-----------|------|--------|
| **package.json** | 手动维护 | `1.0.0` | ✅ 单一数据源 |
| **扩展显示** | 读取 manifest.json | `v1.0.0` | ✅ 来自 package.json |
| **main-latest** | 读取 package.json | `v1.0.0` | ✅ 来自 package.json |
| **文件名** | 读取 package.json | `droplink-v1.0.0.zip` | ✅ 来自 package.json |
| **dev-latest** | Git 提交数 | `1.0.42-dev` | ⚠️ 开发版，无所谓 |

---

## ✅ 最佳实践

### 1. 发布前检查清单

- [ ] 确认 `package.json` 版本号正确
- [ ] 运行 `pnpm build` 确保构建成功
- [ ] 测试扩展功能正常
- [ ] 提交到 main 分支
- [ ] 等待 GitHub Actions 构建完成
- [ ] 验证 Release 页面版本号正确

### 2. 版本号命名建议

```bash
# 开发阶段
1.0.0-alpha.1  # 内部测试
1.0.0-beta.1   # 公开测试
1.0.0-rc.1     # 候选版本

# 正式版本
1.0.0          # 首次发布
1.0.1          # Bug 修复
1.1.0          # 新功能
2.0.0          # 重大更新
```

### 3. 提交信息规范

```bash
# 版本更新提交
git commit -m "chore: bump version to 1.1.0"

# 功能开发提交
git commit -m "feat: add dark mode support"

# Bug 修复提交
git commit -m "fix: resolve login timeout issue"
```

---

## 🚨 常见问题

### Q: 为什么要统一版本号？
**A**: 避免混乱！用户看到的版本号应该和下载的文件名一致。

### Q: 如何回滚版本？
**A**:
```bash
# 方法 1: 修改 package.json 并推送
# 方法 2: 使用 git revert
git revert <commit-hash>
git push origin main
```

### Q: dev 版本号为什么不同？
**A**: dev 版本使用提交计数，方便自动构建。正式版才需要统一。

### Q: 如何查看当前版本？
**A**:
```bash
# 查看 package.json
cat package.json | grep version

# 或使用 node
node -p "require('./package.json').version"
```

---

## 📚 相关文档

- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [npm version 命令](https://docs.npmjs.com/cli/v8/commands/npm-version)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

---

## 🎉 总结

**记住一句话**：`package.json` 是版本号的唯一来源！

修改版本号 → 推送到 main → 自动构建 → 版本号一致 ✅
